import React from "react";

import "./Contact.scss";

const Contact = props => {
  return (
    <section name="section-contact" className="section-contact">
      Kontakt
    </section>
  );
};

export default Contact;
